# oogiri-bot

Slack 上で大喜利を遊べる Bot です 🎍

## ローカル起動

`.env.local` の設定が必要（`.env.sample` 参照）

```shell
yarn
yarn serve
```

## リリース

```shell
yarn release
```

## デプロイ

`.env` の設定が必要（`.env.sample` 参照）

```shell
yarn deploy
```
