{
  "name": "slackbot",
  "version": "2.2.3",
  "description": "slack bot for oogiri",
  "main": "index.js",
  "author": "tocomi",
  "license": "MIT",
  "scripts": {
    "start": "node ./dist/index.js",
    "watch": "tsc-watch --onSuccess 'yarn start'",
    "lint": "eslint --ext .js,.ts ./src",
    "lint:fix": "eslint --ext .js,.ts ./src --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "tsc": "tsc",
    "type-check": "echo '# slackbot type-check' && tsc --noEmit --diagnostics",
    "deploy:heroku": "yarn tsc && yarn start"
  },
  "devDependencies": {
    "typescript": "*"
  },
  "volta": {
    "node": "22.22.1",
    "yarn": "4.0.2"
  },
  "packageManager": "yarn@4.0.2"
}
