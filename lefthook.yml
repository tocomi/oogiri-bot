pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx,js,jsx}'
      run: pnpm lint {staged_files}
    format-check:
      glob: '*.{ts,tsx,js,jsx}'
      run: pnpm format:check {staged_files}
